version: 2.1

executors:
  node-lts:
    working_directory: ~
    docker:
      - image: circleci/node:lts

jobs:
  install:
    executor: node-lts
    steps:
      - checkout
      - restore_cache:
          key: dependency-cache-{{ .Branch }}-{{ checksum "package-lock.json" }}
      - run: npm ci
      - save_cache:
          key: dependency-cache-{{ .Branch }}-{{ checksum "package-lock.json" }}
          paths:
            - node_modules
      - persist_to_workspace:
          root: .
          paths:
            - node_modules
  test:
    executor: node-lts
    steps:
      - checkout
      - attach_workspace:
          at: .
      - run:
          name: Test
          command: npm test

workflows:
  version: 2
  test:
    jobs:
      - install
      - test:
          requires:
            - install
